import React from 'react'

import {AccessAlarm, AirportShuttle, Album, Archive, BatteryAlert, Brush, BusinessCenter} from '@material-ui/icons';
import IconButton from '@material-ui/core/IconButton';

const IconContainer = ({handleButtonClick}) => {
    
    return(
        <>
        <IconButton aria-label="alarm" onClick={() => handleButtonClick(<AccessAlarm/>)}>
            <AccessAlarm/>
        </IconButton>
        <IconButton aria-label="album" onClick={() => handleButtonClick(<Album/>)}>
            <Album/>
        </IconButton>
        <IconButton aria-label="brush" onClick={() => handleButtonClick(<Brush/>)}>
            <Brush/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        <IconButton aria-label="business">
            <BusinessCenter/>
        </IconButton>
        </>
    )

}
export default IconContainer;